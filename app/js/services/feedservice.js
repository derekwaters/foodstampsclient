'use strict';


foodStampsServices.factory('Feed',
	function()
	{
		var feedService = {};
		feedService.getFeed = function(forUser)
		{
			return null;
		}

		return feedService;
	});

